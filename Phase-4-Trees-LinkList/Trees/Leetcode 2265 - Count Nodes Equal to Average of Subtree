/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */

// Approach 1 (Brute Force) -----------------------------------------------------------------(TC = 14ms)
class Solution {
    int cnt = 0;
    int res = 0;
    private int findSum(TreeNode root){
        if(root == null){
            return 0;
        }
        // increase cnt to count total number of node
        cnt++;
        // find sum of left and right nodes 
        int leftSum = findSum(root.left);
        int rightSum = findSum(root.right);
        // return the sum of left and right by including root val
        return root.val + leftSum + rightSum;
    }
    private void dfs(TreeNode root){
        if(root == null){
            return;
        }
        // reset cnt for another subtree
        cnt = 0;
        // find the sum from current root 
        int sum = findSum(root);
        // check the given condition
        if(root.val == sum/cnt){
            res++;
        }
        // recursively find sum of left and right sub tree
        dfs(root.left);
        dfs(root.right);
    }
    public int averageOfSubtree(TreeNode root) {
        dfs(root);
        return res;
    }
}

// Approach 2 -------------------------------------------------------------(BEST TC = 1ms)
class Solution {
    int res = 0;
    private int[] dfs(TreeNode root){
        if(root == null){
            return new int[]{0, 0};
        }
        // go to last node first left than right
        int left[] = dfs(root.left);
        int right[] = dfs(root.right);
        // find sum and cnt for current node 
        int sum = root.val+left[0]+right[0];
        int cnt = 1+left[1]+right[1];
        // check condition match
        if(root.val == sum/cnt){
            res++;
        }
        // return value in pair to upper node
        return new int[]{sum, cnt};
    }
    public int averageOfSubtree(TreeNode root) {
        dfs(root);
        return res;
    }
}

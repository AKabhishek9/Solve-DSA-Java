class Solution {
    public ListNode reverseList(ListNode head){
        ListNode temp = head;
        ListNode prv = null;

        while(temp!=null){
            ListNode next = temp.next;
            temp.next = prv;
            prv = temp;
            temp = next;
        }
        return prv;
    }
}
